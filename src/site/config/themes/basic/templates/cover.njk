{% extends "empty.njk" %}
{% from "parts/macro.njk" import pagesList %}

{% block top %}
<section class="hero is-primary {% if page.page_number < 2 %}is-medium{% endif %}">
  <div class="hero-body">
    <p class="title is-1">{{ config.title }}</p>
    <p class="subtitle">{{ config.description}}</p>
  </div>
</section>
{% endblock %}
{% set allPages = list.getAllFiles() %}
{% set p = makePagination(allPages) %}
{% block content %}
{# content {{ allPages.length }} #}
{% if page.list_page %}
{{ pagesList( page.list_page) }}
{% endif %}
{% endblock %}
